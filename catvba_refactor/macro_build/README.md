# Offline build tooling

规划存放 inventory、resolver、generator、staging、verify-kit 和 audit-catvba 的 Python 实现。

Python 代码属于根 `macro-menu` 项目，不在本目录建立第二个 `pyproject.toml` 或 `uv.lock`。当前尚无
实现，也没有任何 CATIA Compile/运行能力。

